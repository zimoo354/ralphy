{
  "name": "@ralphy/monorepo",
  "private": true,
  "workspaces": ["cli", "dashboard"],
  "scripts": {
    "cli:build": "bun --filter @ralphy/cli build",
    "cli:dev": "bun --filter @ralphy/cli dev",
    "dashboard": "bun --filter @ralphy/dashboard dev"
  }
}