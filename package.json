{
  "name": "@ralphy/monorepo",
  "private": true,
  "workspaces": ["cli", "dashboard"],
  "scripts": {
    "build": "bun --filter @ralphy/cli build",
    "dev": "bun --filter @ralphy/dashboard dev"
  }
}